/**
 * bQuery DevTools — devtools page. Registers the "bQuery" panel.
 */
chrome.devtools.panels.create('bQuery', '', 'panel.html', () => {
  // Panel created; panel.js drives the connection and UI.
});
